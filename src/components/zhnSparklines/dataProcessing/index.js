
import max  from './max';
import min  from './min';
import mean  from './mean';
import median  from './median';

export const hm = {
  max: max,
  min: min,
  mean: mean,
  avg: mean,
  median: median
}
