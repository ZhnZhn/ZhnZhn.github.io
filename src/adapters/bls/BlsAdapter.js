import { crAdapterType1 } from '../crAdapterType1';
import {
  crData,
  crConfOption
} from './fnAdapter';

const BlsAdapter = crAdapterType1({
  crData,
  crConfOption
});

export default BlsAdapter
